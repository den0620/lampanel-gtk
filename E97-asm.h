#include <stdint.h> // for 16 bit int, works not on all machines (see https://stackoverflow.com/questions/24137142/bitwise-operations-on-a-16-bit-number)

uint64_t runE97(uint16_t lampsMatrix[8], char *code){
  // lampsMatrix is pointer to original in main
  uint16_t r0;
  uint16_t r1;
  uint16_t r2;
  uint16_t r3;
  uint64_t registors; // 4 16 bit registors glued together into 64 bit int for return

  // Runtime code

  // free(code);
  return registors;
}
