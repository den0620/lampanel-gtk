#include <stdint.h> // for 16 bit int, works not on all machines (see https://stackoverflow.com/questions/24137142/bitwise-operations-on-a-16-bit-number)

int64_t runE97(char *code){
  int16_t r0 = 0;
  int16_t r1 = 0;
  int16_t r2 = 0;
  int16_t r3 = 0;
  int64_t registors; // 4 16 bit registors glued together into 64 bit int for return

  // Runtime code

  return registors;
}
